import React from 'react';

const InfoCard = () => {
  return (
    <div elevation={3} style={{ textAlign: 'center', padding: '0 10%' }}>
      Add Amount in Category for date
    </div>
  );
};

export default InfoCard;
